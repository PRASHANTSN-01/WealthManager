{
  "name": "wealthmanager-backend",
  "version": "1.0.0",
  "description": "Portfolio management application backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install"
  },
  "keywords": [
    "portfolio",
    "finance",
    "stocks",
    "investment"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
